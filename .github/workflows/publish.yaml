name: Publish

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'tag_name'
        required: true
      registry:
        description: 'bazel central registry target'
        required: false
        default: 'publish-to-bcr-dev/bazel-central-registry'        

permissions:
  id-token: write
  attestations: write

jobs:
  publish:
    uses: publish-to-bcr-dev/.github/.github/workflows/publish.yaml@eb69d7bb9daddbfb0bb6373681407d683462d2b2
    with:
      tag_name: ${{ inputs.tag_name }}
      registry: ${{ inputs.registry }}